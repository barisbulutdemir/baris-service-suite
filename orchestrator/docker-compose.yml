version: '3.8'

services:
  hbbs:
    image: rustdesk/rustdesk-server:latest
    container_name: rustdesk-hbbs
    command: hbbs -r 3.73.144.148:21117 -k _
    volumes:
      - ./rustdesk-data:/root
    network_mode: "host"
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  hbbr:
    image: rustdesk/rustdesk-server:latest
    container_name: rustdesk-hbbr
    command: hbbr -k _
    volumes:
      - ./rustdesk-data:/root
    network_mode: "host"
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  orchestrator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: baris-orchestrator
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - AUTH_TOKEN=BarisServis2026!
      # WebRTC STUN/TURN Server Configuration (Open Relay Project)
      - TURN_URL=turn:global.relay.metered.ca:80
      - TURN_USERNAME=66cdbb717572b6f254590fe1
      - TURN_PASSWORD=vKpbPSAwVXUUtogd
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
